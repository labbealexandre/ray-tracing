#include "input.hpp"

using namespace std;

void loadInputs(bool& rayLang, int& specular, vector<float>& ambiant, Camera& camera, vector<LightSource*>& sources, vector<SceneBaseObject*>& scene, int& n, int& m) {
	rayLang = false;
}